# 数独游戏
给当前项目添加一个数独游戏功能，UI风格和当前项目保持一致。
## 核心功能
### 游戏核心规则与流程

数独游戏的目标是在一个9x9的网格中填入数字1-9，并满足以下三条核心规则：

1.  行规则：每一行必须包含数字1-9，且不能重复。
2.  列规则：每一列必须包含数字1-9，且不能重复。
3.  宫规则：每一个用粗线标记的3x3小九宫格（全盘共9宫）必须包含数字1-9，且不能重复。

游戏流程为：系统生成一个包含部分预填数字的谜题，玩家通过逻辑推理填写空白格，直至所有空格被正确填满，游戏即告成功。
### 后端 (Spring Boot) 设计要点

后端是游戏的大脑，负责维护游戏状态、生成谜题和验证答案。

1.  核心数据模型
    SudokuGame：游戏主状态。包含游戏ID、一个9x9的二维数组puzzle（存储初始谜题，0代表空格）、一个9x9的二维数组solution（存储完整答案）、一个9x9的二维数组userBoard（存储玩家当前填入的数字）、以及游戏状态（进行中、成功、失败）。

     Move：代表一步操作。包含位置（行、列）和填入的数字（0表示清除）。

2.  核心服务与逻辑
    谜题生成与求解引擎：这是数独游戏的核心算法。

        ▪   求解算法：通常使用回溯算法（Backtracking）。该算法能有效地找到一个有效解（如果存在），同时也是生成新谜题的基础。

        ▪   谜题生成：首先利用回溯算法生成一个完整的、符合规则的数独终盘。然后，根据设定的难度等级，从终盘中对称地、随机地挖去一定数量的数字，形成谜题。挖去的数字越多，通常难度越高。

     游戏状态服务：管理游戏的完整状态。当玩家前端发起请求时（如获取新谜题、验证一步操作），该服务负责调用相应的引擎进行处理，并更新userBoard。

     验证逻辑：每当玩家填入一个数字，后端需要实时检查其合法性。这包括检查当前数字在行、列、宫内是否唯一。此外，应提供一个“检查答案”接口，可验证当前userBoard是否与solution完全匹配。

3.  关键API接口
     GET /api/game/new?difficulty=中等：生成一个新的数独谜题，并返回初始游戏状态。

     POST /api/game/{gameId}/move：处理玩家的一次落子。请求体包含Move信息，后端验证后更新userBoard并返回更新后的状态。

    GET /api/game/{gameId}/validate：验证玩家当前的解答是否正确。

    GET /api/game/{gameId}/hint：为玩家提供一个提示，例如随机揭示一个空白格的正确答案。

### 前端 (Vue 3) 设计要点
前端是游戏的脸面，负责渲染界面、响应用户交互，并与后端通信。

1.  核心组件结构
    SudokuBoard.vue：棋盘主组件。使用CSS Grid布局渲染9x9的网格，并区分3x3宫格的粗边框。

    SudokuCell.vue：单个格子组件。根据传入的cellValue（0-9）和isPrefilled（是否为预填数字）属性，决定显示内容和样式（如预填数字通常为黑色且不可编辑，玩家填入的数字可为蓝色）。

     GameControls.vue：游戏控制组件，包含“新游戏”、“检查答案”、“获取提示”、“清除”等按钮。

    GameInfo.vue：显示游戏信息，如计时器、错误次数、当前难度。

2.  状态管理 (推荐使用 Pinia)
    定义一个useSudokuStore来集中管理游戏状态，与后端的SudokuGame模型保持同步。
    // 示例状态
    state: () => ({
      gameId: null,
      puzzle: [], // 初始谜题
      userBoard: [], // 玩家当前棋盘
      solution: [], // 完整答案（通常前端不直接显示，用于提示和验证）
      gameStatus: 'PLAYING'
    })
    

3.  用户交互与视觉效果
    输入方式：玩家可通过键盘数字键直接输入，或点击格子后从屏幕下方的数字面板选择。

    视觉反馈：

        ▪   高亮提示：当选中一个格子时，高亮其所在行、列、宫，提升可读性。

        ▪   冲突检测：实时检查玩家输入，若与现有数字冲突（违反三规则），将该数字标记为红色。

        ▪   候选数（高级功能）：允许玩家在格子内标注铅笔迹（小字号的候选数字），这需要前端组件支持切换“填数”和“标注”模式。

###前后端数据流协同

下表清晰地展示了游戏过程中几个关键交互场景下，前端和后端如何协同工作：

前端 (Vue 3) 事件 后端 (Spring Boot) API/处理 前端响应与更新

页面加载/点击“新游戏” GET /api/game/new 接收初始的puzzle和userBoard，渲染棋盘，重置计时器。

玩家在单元格输入数字 POST /api/game/{gameId}/move (Body包含{row, col, number}) 接收更新后的userBoard和gameStatus。更新界面，若游戏状态变为“SUCCESS”，弹出胜利提示。

玩家点击“检查答案” GET /api/game/{gameId}/validate 接收验证结果，提示玩家答案正确或错误位置。

玩家点击“提示” GET /api/game/{gameId}/hint 接收一个正确数字及其位置，更新userBoard并高亮显示该格子。

### 进阶功能与优化建议

1.  难度分级：根据挖空数字的数量和布局复杂度，设置“简单”、“中等”、“困难”等级别。
2.  解题技巧提示：集成更复杂的数独逻辑（如“唯一候选数法”、“区块摒除法”等），在玩家求助时不仅给答案，还能解释推理过程。
3.  性能优化：谜题生成与求解算法可能耗时，可考虑将其设计为异步任务，或预生成一批谜题存入缓存，避免玩家等待。

