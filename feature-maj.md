# 二人麻将
在当前项目添加一个二人麻将功能，UI风格和当前项目保持一致。
## 核心功能
### 游戏核心规则与流程

首先，我们明确二人麻将与传统四人麻将的主要区别，这是所有逻辑的基石。

•   用牌特点：游戏通常使用万子牌（一至九万，各4张，共36张）、风牌（东、南、西、北，各4张，共16张）、箭牌（中、发、白，各4张，共12张)以及花牌(8张)，合计72张牌。去除了筒子和条子，使牌型组合更紧凑，节奏更快 。

•   核心目标：玩家通过抓牌、出牌、吃、碰、杠等操作，将手牌组合成符合胡牌牌型的组合（通常为4组顺子/刻子加1对将牌），率先胡牌者获胜 。

•   基本流程：

    1.  开局：系统码牌，玩家掷骰决定开门位置和庄家。庄家拥有14张手牌，闲家13张 。
    2.  行牌：玩家轮流进行抓牌、出牌操作，并可对对方打出的牌进行吃（仅能吃上家打出的牌组成顺子）、碰（可碰任何一家打出的牌组成刻子）、杠（明杠或暗杠）等操作 。
    3.  胡牌与结算：当某位玩家的手牌构成合法的胡牌牌型时，即可胡牌。系统根据其牌型计算番数，并进行分数结算 。

###后端 (Spring Boot) 设计要点

后端是游戏规则的守护者，负责核心逻辑运算、状态管理和AI决策。

1.  核心数据模型
    ◦   MahjongGame：游戏状态主体。应包含游戏ID、当前牌墙（可用列表表示）、两位玩家（player1 和 player2，可区分人类玩家和AI）的手牌列表、已打出的牌（牌池）、当前回合玩家、庄家信息、游戏状态（进行中、已胡牌、流局）等。

    ◦   MahjongTile：单张麻将牌模型。需定义其唯一标识、类型（万、风、箭等）、数值（如一万为1，东风可定义为常量）等属性。

    ◦   PlayerAction：玩家动作请求。封装动作类型（出牌、吃、碰、杠、胡）、关联的牌、目标位置等信息。

2.  关键服务与逻辑
    ◦   游戏初始化服务：创建游戏实例，初始化72张牌并洗牌，按照规则为两名玩家发牌，确定庄家 。

    ◦   牌墙管理：使用一个列表（如 List）模拟牌墙。提供从牌墙末尾抓牌、以及处理杠牌后从牌墙末尾补牌（刮风下雨）的逻辑。

    ◦   动作合法性校验引擎：这是核心。需对接收到的每一步玩家动作进行严格校验 。

        ▪   胡牌校验：判断当前手牌是否符合胡牌牌型。可预先定义所有可能的胡牌牌型（如平胡、碰碰胡、七对、清一色等）及其组合规则 。

        ▪   吃碰杠校验：根据游戏规则，判断玩家发出的吃、碰、杠动作是否合法（例如，吃牌只能使用上家打出的牌组成顺子）。

    ◦   番种计算器：胡牌后，根据牌型（如清一色、碰碰胡、风牌刻子、箭牌刻子等）、胡牌方式（自摸、点炮）、特殊规则（杠上开花、海底捞月等）计算总番数 。可参考国标番种，但需适配二人麻将的牌型特点。

    ◦   AI决策引擎（电脑对手）：

        ▪   策略核心：AI需要评估当前手牌，做出摸牌、出牌、吃碰杠的决定。基础策略可包括“听牌优先”（尽快使手牌进入听牌状态）和“防守规避”（避免打出生张或可能点炮的牌）。

        ▪   实现层次：从简单的随机出牌，到基于规则（如优先打出手牌中孤张的风箭牌或中张牌），再到使用概率模型或简单的价值评估函数来选择最优动作。

3.  API接口设计
    ◦   POST /api/game/start：开始新游戏，返回初始游戏状态。

    ◦   POST /api/game/{gameId}/action：处理玩家动作（出牌、吃、碰、杠、胡）。后端校验并执行动作，更新游戏状态，并触发AI响应（若轮到AI）。

    ◦   GET /api/game/{gameId}/state：获取当前游戏状态。

### 前端 (Vue 3) 设计要点

前端负责渲染界面、响应用户交互，并提供流畅的游戏体验。

1.  核心组件结构
    ◦   GameTable.vue：主游戏场景，包含牌桌、两位玩家的手牌区和已打出牌的区域。

    ◦   PlayerHand.vue：玩家手牌组件，负责渲染和管理一位玩家的手牌。需支持点击选牌、拖放出牌等交互。

    ◦   DiscardPool.vue：牌池组件，展示双方已打出的牌，按出牌顺序排列。

    ◦   GameControls.vue：操作按钮组件，当有可进行的吃、碰、杠、胡等动作时，显示相应按钮供玩家选择。

    ◦   GameInfo.vue：信息面板，显示当前回合、得分、番种提示等。

2.  状态管理 (推荐 Pinia)
    使用Pinia Store来集中管理与后端同步的游戏状态。
    // 示例状态 (Pinia Store)
    state: () => ({
      gameId: null,
      playerHand: [], // 当前玩家手牌
      opponentHandCount: 0, // 对手手牌数（可能不公开）
      discardPool: [], // 牌池
      currentTurn: 'player1', // 当前行棋方
      allowedActions: [], // 当前可执行的操作（如可吃、可碰）
      gameStatus: 'PLAYING'
    })
    

3.  用户交互与动画
    ◦   操作流程：玩家点击手牌 -> 前端提示出牌或显示可操作按钮（吃碰杠）-> 调用API -> 接收新状态并更新视图。

    ◦   动画效果：为抓牌、出牌、吃碰杠等操作添加平滑的CSS过渡动画，增强体验。

    ◦   音效反馈：在关键操作（如摸牌、出牌、胡牌）时提供相应的音效。

###前后端数据流协同

前端 (Vue 3) 事件 后端 (Spring Boot) 处理 前端响应更新

玩家点击“开始游戏” POST /api/game/start 初始化牌局 接收初始状态，渲染棋盘和手牌

玩家选择一张牌并点击“出牌” POST /api/game/{gameId}/action (动作类型:出牌) 更新手牌列表，将打出的牌放入牌池，切换回合

对手打出一张牌，玩家有牌可“碰” POST /api/game/{gameId}/action (动作类型:碰) 前端展示“碰”按钮，玩家点击后，后端校验并更新牌型，前端更新界面

玩家胡牌 POST /api/game/{gameId}/action (动作类型:胡牌) 后端校验胡牌牌型并计算番数，返回结果。前端展示胡牌动画和结算信息

### 进阶实现与优化建议

1.  AI强度可调：提供不同难度的AI，例如简单AI随机出牌，中级AI基于简单规则，高级AI可能使用蒙特卡洛树搜索等算法。
2.  游戏回放与存档：记录每一步操作，支持游戏存档和回放功能。
3.  断线重连：通过/api/game/{gameId}/state接口，在断线重连后恢复游戏状态。
4.  性能优化：胡牌判定等算法可能较复杂，注意优化性能。对于AI计算，可考虑异步任务处理。

