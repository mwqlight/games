spring:
  application:
    name: game-backend
  datasource:
    url: jdbc:mysql://localhost:3306/lgames?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0
  ai:
    alibaba:
      dashscope:
        api-key: ${DASHSCOPE_API_KEY:}
server:
  port: 8082
  servlet:
    context-path: /api