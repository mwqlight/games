# 贪吃蛇
在当前项目添加一个贪吃蛇的单机游戏，游戏UI风格，沿用当前的UI风格即可。
## 核心功能

### 游戏核心机制与规则定义
首先，我们需要明确游戏的基本规则和运转方式，这是所有代码实现的基础。
1.  基本目标：玩家通过键盘控制蛇的移动，使其吃到随机出现的食物。每吃到一个食物，蛇身增长一节，玩家得分增加。游戏持续进行，直到蛇头撞到游戏区域边界或自身的身体部位，则游戏结束 。
2.  核心循环：
    初始化：创建游戏画布，初始化蛇（通常为2-3节，初始移动方向默认向右）和第一个食物的位置 。

    玩家输入：监听键盘事件（如上、下、左、右方向键），改变蛇的移动方向。需要注意的一个关键限制是：蛇不能瞬间反向移动（例如，正在向右移动时不能直接按左键转向）。
    蛇的移动：在每一个游戏时间刻度中，蛇头向当前方向移动一格。蛇身的每一节都移动到前一节所在的位置，从而形成整体移动的效果 。
    碰撞检测：
        与食物碰撞：检测蛇头是否移动到了食物的位置。如果是，则蛇身长度增加（通常是在蛇尾添加一节），分数增加，并在空白位置生成新的食物 。
        与边界或自身碰撞：持续检测蛇头是否撞到了画布的边界或者蛇身的任何一节。如果发生碰撞，则游戏状态变为结束 。
    状态更新与渲染：根据最新的游戏状态（蛇的位置、食物位置、分数等）重新绘制整个游戏画面。

### 后端 (SpringBoot) 设计要点

后端主要负责游戏逻辑的运算、状态维护和提供API接口。

1.  游戏状态管理：
    ◦   使用一个游戏状态对象来维护整个对局的信息是核心。这个对象应包括：

        ▪   gameId: 游戏会话的唯一标识。

        ▪   snakeBody: 一个集合（如List<Position>），按顺序存储蛇身每一节的坐标。集合的第一个元素代表蛇头，最后一个元素代表蛇尾。

        ▪   foodPosition: 食物所在的坐标。

        ▪   currentDirection: 蛇当前的移动方向。

        ▪   score: 当前得分。

        ▪   gameStatus: 游戏状态，如PLAYING, GAME_OVER。

    ◦   这个状态对象保存在服务器内存中，例如通过一个ConcurrentHashMap来管理，键为gameId 。

2.  关键API接口设计：
    POST /api/game/start: 开始一个新游戏。后端初始化游戏状态，并返回初始状态（如蛇的起始位置、食物位置、gameId）。

    POST /api/game/move: 接收前端发送的移动指令（如{ "gameId": "123", "direction": "RIGHT" }）。后端根据指令更新蛇的位置，执行碰撞检测和胜负判断，最后将更新后的完整游戏状态返回给前端 。

    GET /api/game/{gameId}/state: 获取指定游戏的当前状态，可用于前端刷新后恢复状态。

3.  核心逻辑服务：
    移动逻辑：根据当前方向计算蛇头的新位置。通过“去尾加头”的方式模拟移动：先根据当前方向计算出蛇头的新位置，将其加入蛇身列表的首位，然后移除列表的最后一位（蛇尾）。如果吃到了食物，则不移除蛇尾，从而实现身体增长 。

    碰撞检测逻辑：计算新蛇头坐标后，立即检查该坐标是否与游戏边界或蛇身列表中的其他坐标重叠。

    食物生成逻辑：当食物被吃后，需要在棋盘的空闲位置（即不在蛇身上的位置）随机生成一个新的食物坐标。

### 前端 (Vue 3) 设计要点

前端负责渲染界面、捕获用户输入并与后端通信。

1.  视图渲染：
    使用 HTML5 Canvas 或 CSS Grid 来绘制游戏网格。Canvas在性能上更有优势，适合这种需要频繁重绘的动画场景 。

    在每一个渲染帧中，根据从后端获取的最新游戏状态，清空画布，然后依次绘制食物、蛇身（通常蛇头可以用不同颜色区分）。

2.  状态管理与组件交互：
     使用 Pinia 或 Vue的响应式系统 来管理前端应用状态，例如当前游戏状态、得分、是否正在等待服务器响应等。

    核心组件可以包括GameCanvas（游戏画布）、ScoreBoard（计分板）和GameController（游戏控制按钮）。

3.  用户交互与实时性：
     通过监听键盘的keydown事件来捕获方向键操作，并立即调用后端的/move接口 。

### 前后端数据流协同

下表概述了游戏进行过程中几个典型交互场景的数据流，帮助你理解系统如何协作：

前端事件 (Vue) 后端API/处理 前端响应更新

页面加载/点击开始 POST /api/game/start 接收初始游戏状态，开始渲染第一帧，并开始监听键盘事件

玩家按下方向键 POST /api/game/move (携带方向和gameId) 接收更新后的游戏状态，重新渲染画面。如果状态为GAME_OVER，则显示结束界面

蛇吃到食物 后端在/move处理中生成新食物，更新分数和蛇身 前端渲染新食物位置、更长的蛇身和更新后的分数

希望这份关于贪吃蛇游戏核心逻辑的提示词，能帮助你清晰地规划开发工作！如果你对特定细节有更多疑问，我们可以继续深入讨论。