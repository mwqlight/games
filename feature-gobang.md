# 五子棋
在当前项目添加一个五子棋的游戏，注意沿用当前的UI风格，还有注意和用户的交互体验，当前实现的是单机游戏，和电脑进行对战。
## 核心功能
1.  游戏规则与流程
    棋盘：标准15x15网格棋盘。
    目标：玩家与AI交替落子，任何一方在横向、纵向或两条对角线上率先形成连续的五颗同色棋子即为获胜。
    流程：游戏开始后，通常玩家（黑子）先行。每回合在空交叉点落子，随后立即进行胜负判定。若未分出胜负，则切换行棋方。游戏需处理玩家回合与AI回合的转换。
2.  核心数据模型
    棋盘表示：使用15x15的二维数组（int[][]或number[][]）表示棋盘状态。通常用0表示空位，1表示黑子（玩家），2表示白子（AI）。
    游戏状态：需要一个对象来管理当前棋局，包括棋盘数据、当前行棋方、游戏状态（进行中、玩家胜利、AI胜利、平局）以及最后落子位置等信息。
3.  胜负判定算法
    核心思路：每次落子后，仅以该落子点为中心，检查其四个方向（水平、垂直、主对角线、副对角线）是否存在连续的五颗同色棋子。这是最高效的方法，无需全盘扫描。
    实现方法：为每个方向定义增量向量（如水平方向为(1,0)）。从落子点出发，分别向两个相反方向计数，累计连续的同色棋子数量。若任一方向上的计数达到或超过5，则判定获胜。需注意处理棋盘边界。
4.  AI策略设计
    基础策略（随机落子）：AI在所有合法空位中随机选择一个位置落子。实现简单，但强度很低。
    进阶策略（贪心算法）：为棋盘上每个空位设计一个评估函数，根据其周边棋型（如是否形成活四、冲四、活三等）进行打分。AI选择分数最高的位置落子。强度有所提升，但仍缺乏深度谋划。
    高级策略（搜索算法）：采用Minimax算法并配合Alpha-Beta剪枝优化。AI会模拟未来若干步（搜索深度）内所有可能的走法，并选择对自身最有利的路径。这是实现较强AI的经典方法，但计算复杂度随深度增加而显著提高。

### 关键注意事项
1.  前后端职责分离
    后端(SpringBoot)：应专注于核心游戏逻辑，包括维护棋盘状态、执行胜负判定、实现AI决策算法。提供RESTful API，如POST /api/move处理玩家落子并返回更新后的游戏状态（包含AI的应对）。
    前端(Vue3)：负责渲染棋盘和棋子、响应用户点击事件、管理UI状态（如当前行棋方提示、高亮显示获胜五子）、提供游戏控制（如重新开始）。通过Axios等工具与后端API交互。
2.  状态管理与同步
    使用Pinia或Vue的响应式系统集中管理游戏状态，确保棋盘视图、状态提示等组件能随游戏状态变化而自动、一致地更新。
    确保前后端对棋盘坐标系的定义一致（例如，原点(0,0)是左上角还是左下角）。
3.  用户体验(UX)优化
    视觉反馈：为棋子落子添加平滑的动画效果。当一方获胜时，高亮显示连成五子的棋子，提升视觉效果。
    交互限制：在AI思考（计算）期间，应禁用棋盘点击事件或显示加载状态，防止用户连续操作。
    异常处理：妥善处理非法落子（如位置已被占用、坐标越界），并给用户明确的提示。

