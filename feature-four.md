# 四子棋
在当前项目添加一个四子棋的单机游戏，游戏UI风格，沿用当前的UI风格即可。
## 核心功能
### 游戏核心规则

首先，我们明确游戏的基本规则和目标，这是所有逻辑的基石。

•   游戏目标：两位玩家（一位是人类玩家，另一位是电脑AI）在一个6行7列的垂直棋盘上轮流落子。任何一方率先在横向、纵向或两条对角线上任一方向连成四个自己的棋子，即获胜。

•   核心流程：

    1.  落子规则：棋子受重力影响，必须从每一列的最底部依次向上堆积。玩家选择一列后，棋子会落于该列最低的空位上。
    2.  回合交替：玩家与AI轮流落子。
    3.  终止条件：当一方达成四连，或整个棋盘被填满（平局）时，游戏结束。

### 后端 (Spring Boot) 设计要点

后端是游戏引擎，负责维护游戏状态、执行核心逻辑（如胜负判定、AI决策）并通过API接口与前端交互。

1.  核心数据模型
    ◦   GameState：游戏状态对象。应包含游戏ID、一个6x7的二维数组（如 int[][]）来表示棋盘（每个元素可为0-空、1-玩家、2-AI）、当前行棋方、游戏状态（进行中、玩家胜利、AI胜利、平局）等信息。

    ◦   MoveRequest：接收前端传来的落子请求，通常包含游戏ID和玩家选择的列号。

2.  关键服务与逻辑
    ◦   游戏初始化服务：创建新的游戏会话，初始化一个6x7的空棋盘，并设定先行方（通常为玩家）。

    ◦   落子逻辑与验证：

        ▪   合法性校验：接收玩家选择的列号后，首先校验该列是否未满。

        ▪   落子执行：校验通过后，找到该列中最低的空位，将当前玩家的标识（1或2）填入棋盘数组。

    ◦   胜负判定引擎：这是核心逻辑。每次落子后，以该落子位置为中心，检查四个方向：

        ▪   水平方向：计算该行向左和向右的连续同色棋子数。

        ▪   垂直方向：计算该列向下（因为从下往上堆叠，通常只需向上检查）的连续同色棋子数。

        ▪   两条对角线：计算两条斜线（左上-右下，右上-左下）上的连续同色棋子数。

        若任一方向连续棋子数达到4，则判定当前行棋方获胜。
    ◦   平局判定：在确认无人获胜后，检查棋盘是否已被完全填满（即所有列都已满）。若已满，则判定为平局。

    ◦   AI决策引擎：实现电脑AI的落子逻辑。

        ▪   基础策略（规则优先）：AI的决策应遵循一个清晰的优先级链条：

            1.  制胜：检查AI是否存在一步制胜的可能（即AI再下一子即可连成四子）。
            2.  阻挡：检查玩家是否存在一步制胜的可能，若有，则落子阻挡。
            3.  抢占中心：优先选择中间几列落子，因为中心位置在连线时拥有更多可能性。
            4.  随机落子：若以上情况均不满足，则从剩余的空列中随机选择。
        ▪   进阶策略（可选）：可引入Minimax算法或蒙特卡洛树搜索（MCTS） 算法，使AI具备一定的前瞻性和策略性。

3.  API接口设计
    ◦   POST /api/game/start：开始一场新游戏，返回初始化的游戏状态（GameState）。

    ◦   POST /api/game/move：处理玩家的落子请求。请求体包含列号，后端处理（包括落子、胜负判定、AI响应）后，返回更新后的完整游戏状态。

    ◦   GET /api/game/{gameId}/state：获取指定游戏的当前状态，可用于前端刷新后恢复状态。

### 前端 (Vue 3) 设计要点

前端负责渲染界面、捕获用户输入，并通过响应式更新提供流畅的游戏体验。

1.  组件结构
    ◦   GameBoard.vue：棋盘主组件。使用CSS Grid布局渲染7列x6行的网格，列容器需设置为flex-direction: column-reverse，以便棋子从底部开始堆积。

    ◦   GameCell.vue：单个格子组件。根据传入的prop（如cellValue）显示空、玩家棋子或AI棋子（通常用不同颜色区分）。

    ◦   GameStatus.vue：显示游戏信息，如当前行棋方、获胜/平局信息。

    ◦   GameControls.vue：包含“重新开始”等按钮。

2.  状态管理 (推荐使用 Pinia)
    定义一个useGameStore来集中管理与后端同步的游戏状态。
    // 示例状态 (Pinia Store)
    state: () => ({
      gameId: null,
      board: [][], // 6x7棋盘状态
      currentPlayer: 1, // 1为玩家，2为AI
      gameStatus: 'PLAYING' // 'PLAYING', 'PLAYER_WON', 'AI_WON', 'DRAW'
    })
    

3.  用户交互与视觉效果
    ◦   落子流程：玩家点击某一列 -> 前端调用/api/game/move接口 -> 接收后端返回的新状态（包含棋盘更新和AI的应对） -> 更新Pinia状态 -> 界面自动重新渲染。

    ◦   视觉反馈：

        ▪   棋子下落动画：为棋子的出现添加从列顶部落至底部的CSS过渡动画，增强物理感。

        ▪   高亮提示：当一方获胜时，高亮显示连成一线的四个棋子。

    ◦   输入限制：在AI思考（即等待后端响应）或游戏已结束时，应禁用棋盘点击事件，防止误操作。

### 前后端数据流协同

为了让您更直观地理解系统如何协作，下表概括了游戏进行中的关键交互场景：

前端 (Vue 3) 事件 后端 (Spring Boot) 处理 前端响应更新

页面加载/点击“新游戏” POST /api/game/start 初始化游戏状态 接收初始GameState，渲染空棋盘，重置状态。

玩家点击某一列 POST /api/game/move (Body包含列号) 接收更新后的GameState（包含AI的落子），更新界面。若游戏状态变为结束，显示结果。

页面刷新或恢复 GET /api/game/{gameId}/state 获取当前游戏状态，完整还原游戏界面。

### 进阶实现与优化建议

1.  胜负判定优化：胜负判定算法应高效。每次落子后，只需以新落子点为中心进行扫描，无需遍历整个棋盘。
2.  AI强度可配置：可以提供不同难度的AI，例如“简单”（纯随机落子或基础规则）和“困难”（使用Minimax或MCTS算法）。
3.  游戏记录与回放：可记录每一步棋，支持游戏复盘功能。
