# 俄罗斯方块
在当前项目添加一个俄罗斯方块的单机游戏，游戏UI风格，沿用当前的UI风格即可。
## 核心功能
###  游戏核心机制与规则

首先，我们明确游戏的基本规则和目标，这是所有逻辑的基石。

•   游戏目标：玩家通过移动、旋转不断下落的各式方块，在底部将其紧密拼接。当水平方向的一行被方块完全填满时，该行便会消除，玩家获得分数。游戏持续进行，直到下落的方块堆叠至场地顶部，无法再放置新方块，则游戏结束。

•   基本规则：

    1.  场地：通常是一个标准大小的网格，例如 10列 x 20行（不包括边界）。
    2.  方块：共有7种不同形状的方块（称为Tetromino），每种方块由4个小方块构成，分别以I, O, T, S, Z, J, L字母的形状命名。
    3.  操作：玩家可以左右移动下落中的方块、旋转方块（通常每次旋转90度），以及加速下落。
    4.  固定与消除：方块下落到底部或与其他已固定的方块接触时，便会固定在场地上。随后检查是否有满行，若有则消除，并让上方行下落填充。
### 后端 (Spring Boot) 核心逻辑

后端是游戏的大脑，负责维护游戏状态、执行核心逻辑和提供API接口。

1.  核心数据模型
    GameState：游戏状态对象。包含游戏ID、一个二维数组grid（表示游戏场地，每个格子存储0-空或颜色值）、当前活动方块currentPiece、下一个方块nextPiece、游戏分数score、游戏状态（进行中PLAYING/已结束GAME_OVER）等。

    Tetromino（方块模型）：描述一个方块的形状（一个4x4的二维矩阵，用0和1表示构成）、类型、颜色、以及其在地图上的坐标(x, y)。

2.  关键服务与逻辑
    游戏初始化服务：创建新的游戏会话，初始化空场地，随机生成当前方块和下一个方块，并设置初始位置（通常从顶部中间出现）。

     碰撞检测系统：这是核心中的核心。在任何移动、旋转操作前，都必须调用此逻辑。它判断方块在目标位置是否与场地边界（墙）或已固定的方块发生重叠。若发生碰撞，则操作无效。

    移动与旋转逻辑：

        ▪   移动：处理左右和向下移动。每次移动后都需进行碰撞检测。向下移动若碰撞，则触发方块固定流程。

        ▪   旋转：实现方块的旋转算法。通常需要为每种形状预定义其4种旋转状态。旋转前需进行碰撞检测，若旋转后位置无效，则不允许旋转（或尝试进行“墙踢”修正）。

    方块固定与行消除逻辑：

        ▪   固定：当方块因碰撞无法继续下落时，将其状态从“活动”转为“固定”，即将方块的每个小方块对应位置写入游戏场地的二维数组grid中。

        ▪   行消除：在方块固定后，从下往上扫描场地，检查每一行是否已被填满。将满行消除，并将该行上方的所有行整体下移。根据一次消除的行数（1行、2行、3行或4行，即Tetris）计算并增加分数。

    游戏结束判断：在新方块生成后，立即检查其初始位置是否与场地已有固定方块发生碰撞。若发生，则判定游戏结束。

3.  API接口设计
    POST /api/game/start：开始新游戏，返回初始游戏状态。

     POST /api/game/move：处理玩家操作（移动、旋转、硬降）。接收操作指令，后端验证操作合法性后更新游戏状态，并返回最新状态。

    GET /api/game/state：获取当前游戏状态（用于前端同步）。
### 前端 (Vue 3) 核心逻辑

前端是游戏的脸面，负责渲染画面、捕获输入并呈现流畅的交互体验。

1.  组件结构
    GameBoard.vue：主游戏画布组件。负责使用CSS Grid或Canvas渲染整个游戏场地（包括已固定的方块和当前正在下落的方块）。

    NextPiecePreview.vue：下一个方块预览组件。

    GameInfo.vue：显示游戏信息，如得分、等级、已消除行数等。

    GameControls.vue：游戏控制组件，如开始、暂停按钮。

2.  状态管理 (推荐 Pinia)
    使用Pinia或Vue的响应式系统来管理从后端获取的游戏状态，确保UI能随状态变化而实时更新。
    // 示例状态
    const gameStore = {
      grid: [], // 游戏场地状态
      currentPiece: null, // 当前活动方块
      score: 0,
      gameStatus: 'PLAYING'
    }
    

3.  用户交互与动画
    输入处理：监听键盘事件（如方向键），并将操作转化为对后端API的调用。需要考虑按键去抖或节流，避免过快操作。

     画面渲染：

        ▪   根据grid数据渲染已固定的方块。

        ▪   根据currentPiece的坐标和形状，实时渲染活动方块。

    动画效果：为方块的移动、旋转、行消除等操作添加平滑的CSS过渡或动画，提升游戏体验。

### 前后端协同工作流

游戏运行时的主要交互流程如下：

前端 (Vue 3) 事件 后端 (Spring Boot) 处理 前端响应更新

页面加载/点击开始 调用 POST /api/game/start 接收初始状态，开始渲染游戏画面，并启动游戏循环（如定时下落）

玩家按下按键（如左移） 调用 POST /api/game/move (携带动作和游戏ID) 接收更新后的游戏状态，重新渲染画面

定时器触发方块自动下落 调用 POST /api/game/move (发送向下移动指令) 接收更新后的状态，重新渲染。若方块固定，则会收到新的当前方块和下一个方块

游戏状态变为 GAME_OVER 在/move或状态检查的响应中返回 停止游戏循环，显示“游戏结束”界面

### 进阶实现与优化建议

1.  游戏循环与计时：前端需要设置一个定时器，控制方块自动下落的速度。速度可随等级提升而加快。
2.  预览与延迟：实现“下一个方块”预览功能。可以考虑加入“硬降”（瞬间落底）和“软降”（加速下落）操作。
3.  持久化与排行榜：可将玩家最高分记录到数据库，并提供排行榜功能。
