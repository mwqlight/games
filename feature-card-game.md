# 纸牌游戏
在当前游戏中，添加一个纸牌抽乌龟的游戏，界面和UI沿用当前游戏的样式
## 核心功能
游戏核心规则 (单机版业务逻辑)
目标：通过抽牌配对，尽快将手牌打完，避免最后持有无法配对的“乌龟牌”。
准备：
使用一副标准扑克牌，移除一张牌（非大小王）作为“乌龟牌”，面朝下放置，玩家和AI均不可见其点数。
将剩余牌洗匀，平均发给玩家和AI。双方各自查看手牌，并丢弃所有成对的牌（点数相同的两张牌）。
回合流程：
玩家回合：玩家从AI的手牌中随机抽取一张。若抽到的牌能与自己手牌凑对，则立即丢弃该对牌。若无法凑对，则保留此牌。
AI回合：AI从玩家的手牌中随机抽取一张。同样，凑对则弃，否则保留。
胜负判定：游戏轮流进行。当一方手牌全部打完时，另一方手持的最后一张无法配对的单张牌即为“乌龟牌”，持有方为输家。
⚙️ 后端设计 (Spring Boot)
核心数据模型：
Card: 单张扑克牌（花色suit、点数rank）。
GameState: 游戏状态（玩家手牌playerCards、AI手牌aiCards、当前回合currentTurn、游戏状态gameStatus、乌龟牌turtleCard）。
关键API接口：
POST /api/game/start: 开始新游戏，初始化牌局，并返回初始状态。
POST /api/game/player/draw: 玩家抽牌。后端执行抽牌逻辑，更新游戏状态，并触发AI的后续操作。
GET /api/game/state: 获取当前游戏状态（用于前端同步和页面刷新后恢复）。
服务层核心逻辑：
GameService:
初始化：创建牌组，指定“乌龟牌”，洗牌，发牌，处理初始配对。
抽牌逻辑：从目标手牌中随机抽取一张，检查是否能与抽牌方手牌配对。
AI策略：AI抽牌可设计为简单随机抽取。可扩展为策略性抽牌（如记忆玩家手牌）。
胜负判定：检查双方手牌数，判定游戏是否结束及获胜方。
🎨 前端设计 (Vue 3)
组件结构：
GameTable.vue：主游戏界面，展示玩家手牌、AI手牌、当前回合提示、游戏结果。
PlayerHand.vue：玩家手牌展示区，支持点击选择牌。
AIHand.vue：AI手牌展示区，通常以背面显示，抽牌时可短暂展示被抽牌。
GameInfo.vue：显示游戏信息，如当前回合、状态提示。
交互与动画：
抽牌动画：当玩家从AI处抽牌或AI从玩家处抽牌时，可添加卡牌移动的平滑动画。
配对提示：抽牌成功配对时，提供视觉反馈（如高亮配对牌）。
回合提示：清晰显示当前是玩家回合还是AI回合，禁用非回合方操作。
🔗 关键交互流程 (序列)
玩家抽牌：
前端：玩家点击“抽牌”按钮 -> 调用 POST /api/game/player/draw。
后端：从aiCards随机抽一张牌加入playerCards-> 检查配对 -> 更新状态 -> 自动执行AI回合逻辑 -> 返回最新游戏状态。
前端：根据响应更新界面，展示抽牌动画和结果。
AI自动抽牌：
后端在处理完玩家抽牌后，立即在同一个请求中执行AI抽牌逻辑，确保状态同步。
前端接收到的响应已包含AI操作后的完整状态。