# 十点半游戏
在当前游戏中，添加一个十点半的游戏，界面和UI沿用当前游戏的样式。
## 游戏功能
一、核心游戏规则 (业务逻辑)
目标：玩家手牌点数之和尽量接近但不超过10.30。
点数计算：
点牌 (A, 2-9, 10)：A计为1点，数字牌按牌面点数计算。
人牌 (J, Q, K)：计为半点。
特殊牌型包括“五小”（5张牌总点数小于10.30，3倍赔率）、“人五小”（5张人牌，4倍赔率）和“天王”（5张牌点数为10.30，5倍赔率）。
流程：系统担任庄家。玩家先获得一张暗牌，随后可选择“要牌”或“停牌”。玩家最多可持有5张牌。若玩家点数超过10.30（爆牌），则庄家获胜。玩家停牌后，庄家按规则要牌，然后与玩家比牌。
胜负与赔率：庄家分别与每位闲家比较。闲家获胜时，根据其牌型获得相应倍率的赔率（普通牌型1倍，十点半2倍，五小3倍，人五小4倍，天王5倍）。庄家获胜则不翻倍。
二、后端设计 (Spring Boot)
核心控制器 (GameController)：
POST /api/game/start：开始新游戏，发一张暗牌。
POST /api/game/hit：玩家要牌，返回新牌并判断是否爆点。
POST /api/game/stand：玩家停牌，触发庄家要牌逻辑并返回最终结果。
服务层 (GameService)：
实现发牌、要牌、点数计算、胜负判定及赔率计算的核心算法。
管理游戏状态（如当前牌堆、玩家手牌、庄家手牌）。
数据模型：
Card：单张牌（花色、点数、类型）。
GameStatus：游戏状态（玩家牌列表、庄家牌列表、当前回合状态）。
三、前端设计 (Vue 3)
组件结构：
GameTable.vue：主游戏界面，展示玩家和庄家的手牌、当前点数、操作按钮（“要牌”、“停牌”）。
Card.vue：单张牌的展示组件，支持暗牌效果。
状态管理 (Pinia)：管理游戏状态（如双方手牌、游戏阶段、得分）。
交互：通过Axios调用后端API，实时更新游戏状态。