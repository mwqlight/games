# 井字游戏
在当前项目添加一个井字游戏功能，UI风格和当前项目保持一致。
## 核心功能
### 游戏核心机制与规则

首先，我们明确游戏的基本规则和目标，这是所有逻辑的基石。

•   游戏目标：两位玩家（一位是人类玩家，另一位是电脑AI）轮流在3x3的网格上放置各自的标记（通常是 X 和 O）。任何一方率先将三个标记连成一条线（横、竖或斜线）即获胜。若棋盘填满仍无人获胜，则为平局。

•   核心流程：游戏开始后，通常由玩家（例如执X）先行。双方交替落子，每次落子后立即进行胜负判定。若分出胜负或平局，游戏结束；否则，切换行棋方，继续游戏。

### 后端 (Spring Boot) 设计要点

后端是游戏引擎，负责维护游戏状态、执行核心逻辑（如胜负判断、AI决策）并通过API接口与前端交互。

1.  核心数据模型
    ◦   GameState：游戏状态对象。应包含游戏ID、一个3x3的二维数组（如 String[][] 或 char[][]）来表示棋盘（每个元素可为 "X", "O", 或 null/空字符串表示空位）、当前行棋方（currentPlayer）、游戏状态（如 PLAYING, X_WON, O_WON, DRAW）等信息。

    ◦   MoveRequest：接收前端传来的落子请求，通常包含游戏ID和落子位置（行、列）。

2.  关键服务与逻辑
    ◦   游戏初始化服务：创建新的游戏会话，初始化一个空的3x3棋盘，并设定先行方。

    ◦   落子逻辑与验证：接收玩家的落子请求后，首先校验位置的合法性（是否在棋盘范围内）以及该位置是否为空。校验通过后，更新棋盘状态。

    ◦   胜负判定引擎：这是核心逻辑。每次落子后，以该位置为中心，检查其所在行、列以及可能的两条对角线，判断是否有三个连续的相同标记。若有，则游戏结束，并宣布获胜方。

    ◦   平局判定：在确认无人获胜后，检查棋盘是否已被填满（即无空位）。若已满，则判定为平局。

    ◦   AI决策引擎（电脑玩家）：实现一个具有基本智能的AI。

        ▪   核心策略：采用优先级规则。AI的决策应遵循一个清晰的优先级链条：

            1.  制胜：检查AI（如O）是否存在一步制胜的可能。
            2.  阻挡：检查玩家（如X）是否存在一步制胜的可能，若有，则落子阻挡。
            3.  占据中心：若中心格为空，优先占据。
            4.  占据角落：其次考虑占据空余的四个角落。
            5.  随机落子：若以上都不满足，则从剩余的空边格中随机选择。
        ▪   进阶（可选）：可引入Minimax算法（可配合Alpha-Beta剪枝优化）来计算最佳落子点，使AI更具挑战性，但这超出了基础优先级规则的范畴。

3.  API接口设计
    ◦   POST /api/game/start：开始一场新游戏，返回初始化的游戏状态（GameState）。

    ◦   POST /api/game/move：处理玩家或AI的落子请求。请求体包含位置信息，后端处理（包括AI自动响应）后，返回更新后的完整游戏状态。

    ◦   GET /api/game/{gameId}/state：获取指定游戏的当前状态，可用于前端刷新后恢复状态。

### 前端 (Vue 3) 设计要点

前端负责渲染界面、捕获用户输入，并通过响应式更新提供流畅的游戏体验。

1.  组件结构
    ◦   GameBoard.vue：棋盘主组件。通常使用CSS Grid布局渲染3x3的网格。

    ◦   GameCell.vue：单个格子组件。根据传入的prop（如mark）显示空、X或O，并响应点击事件。

    ◦   GameStatus.vue：显示游戏信息，如当前行棋方、获胜/平局信息。

    ◦   GameControls.vue：包含“重新开始”等按钮。

2.  状态管理 (推荐使用 Pinia)
    定义一个useGameStore来集中管理与后端同步的游戏状态，这是前后端数据一致性的关键。
    // 示例状态 (Pinia Store)
    state: () => ({
      gameId: null,
      board: [][], // 9宫格棋盘状态
      currentPlayer: null,
      gameStatus: 'PLAYING'
    })
    

3.  用户交互与视觉效果
    ◦   落子流程：玩家点击棋盘空格 -> 前端调用落子API -> 接收后端返回的新状态（包含AI的应对） -> 更新Pinia状态 -> 界面自动重新渲染。

    ◦   视觉反馈：

        ▪   为棋子的出现添加简单的CSS过渡动画，提升体验。

        ▪   当一方获胜时，高亮显示连成一线的那三个格子。

    ◦   输入限制：在AI思考（即等待后端响应）或游戏已结束时，应禁用棋盘点击事件，防止误操作。

### 前后端数据流协同

为了让您更直观地理解系统如何协作，下表概括了游戏进行中的关键交互场景：

前端 (Vue 3) 事件 后端 (Spring Boot) 处理 前端响应更新

页面加载/点击“新游戏” POST /api/game/start 接收初始GameState，渲染空棋盘，重置状态。

玩家点击一个空格 POST /api/game/move (Body包含落子位置) 接收更新后的GameState（包含AI的落子），更新界面。若游戏状态变为结束，显示结果。

玩家点击“重新开始” POST /api/game/start 接收新的初始状态，完全重置游戏。

### 进阶实现与优化建议

1.  前后端状态同步：确保前端Pinia中的状态与后端GameState对象始终保持一致是体验流畅的关键。
2.  AI强度可配置：可以提供不同难度的AI，例如“简单”（纯随机落子）和“中等”（使用上述优先级规则）。
3.  游戏记录：可考虑将棋局记录（每一步的移动）保存下来，支持简单的“复盘”功能。

